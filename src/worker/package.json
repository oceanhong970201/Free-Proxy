{
  "name": "proxy-sub-aggregator-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "predeploy": "npm run typecheck && npm test",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "yaml": "2.9.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260715.1",
    "typescript": "5.9.3",
    "vitest": "4.1.10",
    "wrangler": "4.111.0"
  }
}
